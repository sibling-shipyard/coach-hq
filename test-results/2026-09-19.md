# Test results - 2026-09-19

## Run 1 - 00:30-12:00 UTC, finishing the fix-and-retest cycles from 2026-09-18 (issue #1105)

**Scope decided:** rerun every pair the 2026-09-18 budget outage left unconfirmed, then keep the
loop going on whatever still failed: test -> read the transcript and server trace -> fix in a new
stacked PR with Sentry coverage -> retest only the failures and their neighbours.

**Key:** the OpenRouter key in `ui/.env.local` is the production key. Every live run below spent
production budget. Headroom before stopping: $6.62 of a $30 monthly cap, and that cap is shared with
real athlete traffic. The 2026-09-18 outage was this cap being hit.

### Cycle 6 - stack top #1238, 18 runs: 15 PASS, 3 FAIL

All 9 pairs left unconfirmed by the outage now have a passing latest run:
`session-plan` skanda, prateek and akash (2/2 each), `multi-field-success` prateek and date2022,
`week-kickoff-flash`, `compound-narration-probe` akash (2/2). No budget errors this time.

| Failing pair | What happened |
|---|---|
| `multi-field-success [akash]` turn 1, `workout-lifecycle [akash]` turn 1 | routine claimed built, nothing written |
| `daily-basic [akash]` turn 1 | weight saved, no `coach_log` row |

### Server traces captured for the failures (10 more runs, full logs)

- **`workout-lifecycle [akash]`:** the model's one reprompt was spent on a missing `workout_create`.
  The reprompted reply included the routine but mapped "Pike Push-up" to the `handstand_free`
  progression (`current: 2s`), so the server dropped it: `workout_create: "Pike Push-up" doses 24
  above progression "handstand_free"'s current value`. Nothing could fix it (the reprompt budget is
  shared). Root cause: `requestCoachReply.ts`, one reprompt for every violation.
- **`daily-basic [akash]`:** `stillMissingNote: true` after the reprompt. The model returned no
  `coach_note` twice, so `buildTurnWrites.ts` wrote the profile with no `coach_log` entry.
- `multi-field-success [akash]` passed 6/6 in this pass.

### Fixes (PR #1243, issue #1242, stacked on #1238)

1. A late dose violation gets one more dose-only reprompt, with a hint to drop a mismatched
   `progression_id`. Sentry reports `workoutCreateProgression` only if it survives that pass.
2. A required-note action with no `coach_note` after the reprompt gets a plain fallback note built
   only from the recorded fields, reported to Sentry once per turn as a silent fixup.

The third sibling's review found a real flaw in (2): the note was built before validation dropped
bad actions, so it could say "recorded" for something never saved. It is now built from the
surviving, validated actions only (tests: a dropped `quest_event` is not named; no note when every
required action was dropped).

### Cycle 7 - stack top #1243, 18 runs: 16 PASS, 2 FAIL

Passing: `daily-basic` akash 3/3 and skanda, `workout-lifecycle` akash 4/4, `multi-field-success`
akash 2/3, `session-plan` akash, and the neighbours the note fallback could touch:
`daily-sleep-skip`, `injury-resolve-by-bodypart`, `quest-event`, `pattern-style-sport` and
`season-transition`.

| Failing pair | What happened |
|---|---|
| `multi-field-success [akash]` turn 2 | turn 1's `workout_create` was dropped ("(Note: couldn't save workout_create...)" reached the athlete), so there was no routine to edit; turn 2 was honest about it |
| `workout-lifecycle [skanda]` turn 1 | routine claimed built and saved, nothing written; turn 2 honest ("that routine never actually got locked in") |

**Root cause not confirmed.** In both, the athlete got the honest correction and the coach then
behaved correctly, so nothing false reached them, but the routine still was not built. The raw logs
do not hold the server-side drop reason (the harness output filter dropped it), and finding it needs
another live run on the production key. Known drop reasons are already covered by a reprompt (dose,
missing injury_ack, unknown progression id); an unchecked candidate is the applier's final
`validateWorkout` step. This is now about 2 in 18 runs, down from roughly a third before #1230-#1243.

### Flaky UI test, root cause found and fixed (PR #1245, issue #1244)

`ui/client/src/components/coach-chat/coachChatModel.test.ts` failed a different test on most local
runs with `Test timed out in 5000ms`. Cause: tests that fail a fetch on purpose waited through the
real `fetchWithRetry` backoff (500ms then 1s). The file took 16.5s, and a test with two failing
calls sleeps 3s, so under load it crossed vitest's 5s limit. Test-only fix: backoff sleeps run at
once, the 5s proactive-snapshot abort timer is left alone. The file now runs in about 50ms and passed
5 of 5 runs.

### Cycle 8 - four capped live runs to find out why the last two failures happened

Approved as a small, disclosed spend on the production key (headroom $6.61 before, about $0.19 for
these). Full server traces were kept for the failures.

- **`multi-field-success [akash]` (1 of 2 failed):** the dose reprompt worked. The over-dosed "Pike
  Push-Ups" was re-dosed, but the fix left an exercise without a finite `reps`, so the server dropped
  the routine: `workout schema: workout_create result ... exercises[0].reps should be a finite
  number`. The extra pass from #1243 re-checked only the dose, not the structure.
- **`workout-lifecycle [skanda]` (1 of 2 failed):** `stillMissedWorkoutCreateLanguage` after the
  reprompt, so the model ignored the build request twice. Nothing was dropped.
- Same shape in both: a routine problem survived the one reprompt with nothing left to fix it.
- Also seen: the mobility-session `week_update` on akash was dropped ("a new session already marked
  done was proposed for 2026-09-26 ... session sess_20260926_1 is still planned"). The server
  protected the data and the athlete got the honest correction, so nothing was changed.

**Fix (PR #1248, issue #1247, stacked on #1243):** the extra pass now covers every remaining
`workout_create` problem (dose, structure, no routine at all, missing `injury_ack`) in one more call,
and Sentry reports only what survives it.

### Cycle 9 - stack top #1248, 6 runs: 6 PASS

`multi-field-success` akash 2/2, `workout-lifecycle` skanda 2/2 and akash 1/1, `session-plan`
akash 1/1. No budget errors.

### Follow-up - `fsp-basic` on a genuinely fresh repo, one run

The 09-17 pass ran `fsp-basic` on `coach-skanda-testing`'s `main`, which has `coach_since: 2026-09-01`,
so it exercised returning-athlete mode, not the First Session Protocol. This run fixes that.

**Reset (per `coach-chat-testing.md`, on a scratch branch, never `main`):** branch
`skanda-testing/coach-skanda-testing@test/fsp-fresh-2026-09-19` from `main`, then the five
FSP-owned files (`profile.json`, `memory.json`, `injuries.json`, `seasons.json`, `quests.json`)
overwritten with the blank templates from `platform/scripts/carve-skeleton.mjs`. Verified blank,
and `main` is unchanged (`d431f1e3` before and after).

**Result:** PASS, 6/6, 6 model calls, $0.065 on `google/gemini-3.8-flash` (production key, headroom
$5.83 before). Raw log: `raw/2026-09-19/manual/manual-coach-chat-skanda-testing-coach-skanda-testing-log-12-38-25.json`.

| Turn | Changed files | What the coach did |
|---|---|---|
| 0 greet | none | introduced itself and asked what to call the athlete (first-session greeting) |
| 1 age, weight, height, city | `profile.json` | saved height, weight and timezone, asked for the date of birth |
| 2 goal | `coach_log.json` | acknowledged the sub-2 half marathon goal |
| 3 knee | `injuries.json` | flagged the right knee (1 flag) |
| 4 frequency, level | `memory.json` | recorded 4 days a week, intermediate |
| 5 wrap-up | `quests.json`, `seasons.json` | created the season and the main quest |

**Finding, not a coach bug:** the scripted messages never give a name or a date of birth, so the
first session never completes. `coach_since` stays unset and no first-session benchmark routine is
generated. `fsp-basic` therefore covers the FSP conversation path, but not its completion step. Adding
a name and date of birth turn to `manual-coach-chat-turns-fsp.json` would cover that too.

**Left in place:** the branch `test/fsp-fresh-2026-09-19` is evidence and was not deleted.

### Verdict

Every scenario x repo now has a passing latest run. The round-2 plan doc
(`docs/plans/coach-chat-live-test-round-2.md`) is deleted in this PR, its finishing PR.

Known residual: a routine build is model-dependent, so an occasional turn can still end with an
honest "wasn't saved, ask again" note instead of a routine. That is now reprompted twice, corrected
for the athlete and reported to Sentry, and no run in cycle 9 hit it.

### Cleanup - scratch branches swept

All `test/` and `retest/` scratch branches were deleted from the five athlete repos on 2026-09-19:
508 branches (skanda 185, akash 157, date2022 65, prateek 47, shreyas 54). Kept:
`test/close-verification` on `coach-skanda-2003`, the long-lived verification branch. None had an
open PR or was protected, and every repo's `main` SHA was identical before and after. The name and
commit SHA of every deleted branch is in `raw/2026-09-19/branch-sweep-manifest.tsv`, so any branch
can be recreated. Other branch families (`core/`, `data/`, `claude/`, `coach-chat-test`,
`test_branch`) were not touched. The testing repo `skanda-testing/coach-skanda-testing` was not swept:
the first attempt used the wrong owner and read the 404 as "not reachable". It is reachable and holds
about 130 old scratch branches.

**Cost:** $4.116 across the 77 raw logs kept from today, on the production key.
