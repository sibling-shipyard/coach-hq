# Test results - 2026-09-13

## Run 1 - 12:32-15:37, #999 hardening round: paid eval + manual live verification

**Scope decided:** re-run the golden-dataset eval after `#999` changed `coachPromptText.ts` for the
first time since the 2026-09-08 baseline (ADR 0024), then live-verify both the eval-surfaced fixes
and the earlier review-fix round against a real athlete repo. Full narrative for both halves:
`test-results/raw/historical/PAID_EVAL_AND_MANUAL_VERIFICATION_727.md` and
`test-results/raw/historical/LIVE_VERIFICATION_REVIEW_FIXES_727.md`. 44 raw files total: 24 under
`eval/`, 20 under `manual/`.

### Live eval - paid, model: not recorded in these files, calls not recorded, cost not recorded (predates usage tracking, #1045)

**First full pass** (`test-results/raw/2026-09-13/eval/eval-coach-chat-log-13-26-12.json`, 26
transcripts counting multi-turn separately): 19 passed, 4 failed. All 4 were stale fixtures, not
`#999` regressions - `plan-edit-vs-template-edit-disambiguation`,
`week-plan-kickoff-ritual`, and `session-reconcile-actual-differs` all checked for action fields
`#973` (merged separately, before this stack) had already collapsed into `week_update`; the
fixtures hadn't been updated. `session-reconcile-actual-differs` also had a stale hardcoded session
date. All three were fixed (updated `expect.actionFieldsPresent`, refreshed context strings, added
`{{TODAY}}` token support) and confirmed passing individually
(`eval-coach-chat-log-13-55-04.json`, `-13-55-22.json`, `-13-55-32.json`).

**Full re-run after the fixture fixes** (`eval-coach-chat-log-13-59-02.json`) surfaced two new
real bugs, not fixture rot: `week-plan-kickoff-ritual` (model narrates a full 7-day week in prose,
never calls `week_update`) and `fsp-quest-create-after-profile-complete [turn 2/2]` (goal stated,
model narrates a launched season, but `season_start` and `profile_update` are both empty) - both
matching the silent-structured-output-omission pattern from earlier historical docs.

| Transcript | Result | Notes |
|---|---|---|
| week-plan-kickoff-ritual | FAIL (repro), then FAIL/PASS alternating across reruns | Fixed via a prompt reinforcement plus a new deterministic reprompt, `isProseOnlyWeekPlan` in `coachTurn.ts` (fires on 5+ named weekdays with no `week_update`). Reprompt-only fix confirmed insufficient by 2 reruns still failing (`eval-coach-chat-log-14-11-09.json` through `-14-12-28.json` show the post-fix state settling to PASS) |
| fsp-quest-create-after-profile-complete [turn 2/2] | FAIL (repro `eval-coach-chat-log-14-08-24.json`), PASS after fix (`-14-08-37.json`, `-14-08-46.json`, `-14-18-*.json`) | Fixed via a prompt reinforcement plus `findMissedSeasonLanguage` in `coachTurn.ts`, scoped to the athlete's own goal-declaring language on a first-session turn |
| incremental-injury-disclosure [turn 3/3] | FAIL (in the 13:26 full run) | Investigated, not fixed - `eval-coach-chat.ts` calls `askGemini()` directly and never runs the real write-time dedup guard (`injuryTextsLikelySame`) that would catch this in production. Hand-computed word-overlap for this run's two texts: 8/10 = 0.8, above the 0.5 threshold, so production traffic would be deduped. See `test-results/raw/historical/PAID_EVAL_AND_MANUAL_VERIFICATION_727.md` §"incremental-injury-disclosure [turn 3/3]" |

Isolated single/two-transcript re-run files in between (`eval-coach-chat-log-13-59-40.json`,
`-13-59-51.json`, `-14-00-17.json`, `-14-07-00.json` through `-14-07-27.json`) are the same fixes
being iterated on live; individual PASS/FAIL states track the narrative above.

### Manual live-chat - athlete: skanda-2003, repo: skanda-2003/coach-skanda-2003, model: not recorded (openrouter per the historical doc's header), calls not recorded, cost not recorded (predates usage tracking, #1045)

20 manual log files, all PASS on their content turn (a few hit a `greet` `ERROR` before a
successful retry). Grouped by scenario, matching
`test-results/raw/historical/LIVE_VERIFICATION_REVIEW_FIXES_727.md`:

- `coach_since`/`profile_update` merge (`-15-22-07.json`) - stating a missing timezone on a
  near-complete profile: `coach_since`, `timezone`, and `first_session_benchmark_pending` all
  landed together, confirmed via the historical doc's real commit diff.
- Extra-session terminal-status guard, 5 phrasing variants (`-14-56-01.json`, `-14-57-15.json`,
  `-15-24-32.json`, `-15-26-15.json`, plus a duplicate-session phrasing at `-12-34-33.json`,
  `-12-36-13.json`, `-12-37-03.json`, `-12-37-48.json`, `-12-38-13.json`) - the model logged each
  extra session as `status: "planned"`, never a terminal status on a brand-new entry, so the guard
  never had anything to catch. A real negative result, not a gap - see the historical doc.
- Week-plan-kickoff prose-only reprompt (`-15-28-18.json`, `-15-29-03.json`, `-15-29-53.json`) -
  `isProseOnlyWeekPlan` fired live on 2 of 3 runs and self-corrected; the third surfaced an
  unrelated pre-existing bug (an over-length `intent` field correctly dropped by existing
  validation, out of scope, not fixed here).
- Season-language reprompt (`-15-32-15.json` through `-15-37-08.json`, 6 files) - the first 2
  branches were seeded incorrectly (a setup mistake, not a finding) and don't test the
  first-session path; the 4 properly-seeded runs all set `season_start` correctly on the first
  attempt, so `findMissedSeasonLanguage` never had a live omission to catch either.
- Workout create (`-12-32-31.json`) - one PASS, matches the historical doc's workout-create check.

**Run cost:** cost not recorded (predates usage tracking, #1045). **Day total so far:** same.
