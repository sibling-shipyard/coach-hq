# Test results — 2026-09-14

## Run 1 — manual + unit, injury/habit disclosure language and a repeat baseline conversation

**Scope decided:** 8 single-turn manual scenarios probing injury/habit disclosure and resolution
language, plus 2 full 5-turn baseline conversations (weight, hip soreness, 5k run, wrap-up) and one
unit-suite run. No eval this day. Reason for this specific selection not recorded in the raw logs
themselves - no historical doc covers this date.

### Unit suite

1019 passed, 0 failed. Raw: `test-results/raw/2026-09-14/unit/vitest-results-17-11-32.json`

### Manual live-chat — athlete: skanda-2003, repo: skanda-2003/coach-skanda-2003, model: not recorded (predates usage tracking), calls not recorded, cost not recorded (predates usage tracking, #1045)

All 10 manual files PASS on every turn.

| File                                                                | What it sent                                                                                                   | Files committed                                                                            |
| ------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `manual-coach-chat-skanda-2003-coach-skanda-2003-log-10-50-22.json` | Full profile intake in one message (age/height/weight/timezone)                                                | `profile.json` + chat/log                                                                  |
| `-12-13-09.json`                                                    | "elbow is still a little sore but getting better"                                                              | `injuries.json` + chat/log                                                                 |
| `-12-13-51.json`                                                    | "leg's been a bit sore lately, been babying it"                                                                | chat/log only (no `injuries.json` write)                                                   |
| `-12-14-36.json`                                                    | "the soreness is easing off, feeling a lot better now"                                                         | chat/log only                                                                              |
| `-13-24-23.json`                                                    | "finished cold shower and 6am wake-up, but skipped protein target"                                             | `ledger/progress.json` + chat/log                                                          |
| `-13-24-59.json`                                                    | "knee's feeling a lot better now, but tweaked my ankle"                                                        | `injuries.json` + chat/log                                                                 |
| `-13-41-17.json`                                                    | "started rock climbing regularly this month"                                                                   | `memory.json` + chat/log                                                                   |
| `-13-56-46.json`                                                    | "want to start a new habit of stretching every morning"                                                        | `ledger/quests.json` + chat/log                                                            |
| `-16-34-49.json` and `-16-38-51.json`                               | greet, weight 72kg, hip soreness, 5k run, wrap-up (same 5-turn baseline conversation seen on 2026-08-26/27/30) | `chat_history.json`, `coach_log.json`, `injuries.json`, `profile.json` on the wrap-up turn |

No FAIL/ERROR this day - nothing to root-cause.

No paid section has a usable cost figure: cost not recorded (predates usage tracking, #1045).

## Run 2 — 18:00, verifying PR #823 (Gemini API key as header, not URL query param) against this stack

**Scope decided:** Tech Lead asked vade-the-tester to give PR #823's own unchecked test-plan item
("verify on a Preview deploy against real Gemini") a live local verification instead. Tried to merge
`origin/feat/638-gemini-key-header` (`--no-commit --no-ff`) into this worktree to run its diff through
this stack's own tooling. The merge hit real, structural conflicts, not incidental ones: PR #823
branched off an older `main` before #713/#920/#1045 (this stack's history) moved Gemini calls behind
a provider-neutral `LlmAdapter` seam and relocated `geminiClient.ts`/`coachWorkoutFiles.ts` under
new directories (`_lib/gemini/`, `_lib/decide/`). Git saw those as modify/delete conflicts
(`ui/api/coach-chat/_lib/coachWorkoutFiles.ts`, `ui/api/coach-chat/_lib/geminiClient.ts`,
its test file) plus real content conflicts in `ui/api/_lib/llmAdapters/geminiSoulCache.ts` and
`ui/api/coach-message/_lib/coachMessage.ts` — the latter's conflict spans lines 876-939, where HEAD's
side calls `adapter.generate(...)` and PR #823's side is a raw `fetch` call. Reconciling that would
mean re-authoring application code inside a different abstraction than the one PR #823 was written
against — outside vade-the-tester's scope (never touches application/production code) and not a
"resolve the markers" job. Ran `git merge --abort` per the brief's stop condition, cleanly, no
leftover state (`git status` clean immediately after).

**What this makes moot:** checking `grep -rn "x-goog-api-key"` on HEAD (no merge) found the fix
already landed independently — `ui/api/_lib/llmAdapters/geminiAdapter.ts:153` and
`ui/api/_lib/llmAdapters/geminiSoulCache.ts:128` both send `x-goog-api-key` as a header today, and
`geminiAdapter.ts:3`'s own comment cites `#638`. `git log` traces this to `1b85e4b9` (#1045, ADR-driven
`askGemini` usage work) and earlier to `1329f79c`/`73df61d7` (#713's `LlmClient` seam). A repo-wide
grep for `?key=`/`generateContent\`` on HEAD found exactly 2 Gemini call sites, both header-only — no
lingering query-param usage anywhere in the current app code. PR #823 is not wrong, it's superseded:
its 4 call sites (`geminiClient.ts`, `coachWorkoutFiles.ts`, `soulCache.ts`, `coachMessage.ts`) no
longer exist in that shape on this stack: the fix is already in via a different, larger refactor.
`geminiAdapter.test.ts:54` (`"authenticates with the x-goog-api-key header, not a URL query param"`)
already covers it. One gap PR #823 would have closed that HEAD does not yet have: no test file exists
for `geminiSoulCache.ts` at all (`find ui/api -iname "*geminiSoulCache*"` returns only the source
file) — a real, small coverage gap, flagged for Bob, not fixed here.

**Environment correction to the brief:** the brief assumed a live OpenRouter call would prove the
header-auth change works end to end. It can't. `ui/api/_lib/llmAdapters/openRouterAdapter.ts:150`
authenticates with `Authorization: Bearer <key>` — a completely different mechanism from Gemini's
`x-goog-api-key`. This environment's `GEMINI_API_KEY` returns `403` on a direct ping
(`curl .../generateContent?key=$GEMINI_API_KEY` → `403`), so no live call available here can actually
exercise `x-goog-api-key` against real Gemini. Ran the OpenRouter live scenario anyway as a pipeline
smoke test (it was going to run regardless per the brief, and it's cheap) — it does not, and cannot,
serve as evidence for PR #823's specific header-auth claim.

### Unit suite

1017 passed, 1 pre-existing failed suite (`client/src/hooks/useRepoData.test.ts` — `client/src/data/`
doesn't exist in this worktree, a UI-data-generation gap unrelated to Gemini auth or PR #823, see
AGENTS.md's "UI data files" note). Raw: `test-results/raw/2026-09-14/unit/vitest-results-17-55-32.json`

### Simulation suite — paid, provider: OpenRouter (not Gemini — see correction above), model: not

recorded (same #1045 usage-logging gap as Run 1), calls not recorded, cost not recorded
| Scenario | Result | Notes |
|---|---|---|
| daily-basic | PASS | 5/5 turns PASS, all `confidence: "observed"` real commits to `skanda-2003/coach-skanda-2003` on scratch branch `test/manual-2026-09-14T17-57-16-137Z`. Proves the pipeline works on HEAD; proves nothing about Gemini's `x-goog-api-key` specifically since this call went through OpenRouter's Bearer-token path, not Gemini's. Raw: `test-results/raw/2026-09-14/manual/manual-coach-chat-skanda-2003-coach-skanda-2003-log-17-58-32.json` |

`test-results/coverage-index.json`'s `manual:daily-basic` entry updated automatically by the harness:
`last_pass_sha` now `bf5f1c45468fd0f887542292c470ed6ab0f099ac` — this branch's own HEAD, honest
because the merge was aborted before this run; no PR #823 code was ever part of what actually ran.

**Verdict on PR #823:** not a live-Gemini pass or fail — the direct question its own checklist asked
("does real Gemini accept the header form") is already answered elsewhere on this stack, by a
different implementation that reached the same place first. Recommend closing #823 as superseded by
#713/#1045 rather than merging it, since merging would reintroduce the pre-refactor file layout it
was written against. No new bug found in PR #823's own diff; the diff is well-formed for the `main`
it was branched from, just stale against where this stack's `main`-bound history has already gone.

**Run cost:** $0 recorded (usage doesn't reach this log yet, #1045's known gap). **Day total so far:**
$0 recorded, same gap.

## Run 3 — 18:15, closing 5 real gaps in the vade-the-tester infra (#1053, finishing PR)

**Scope decided:** Tech Lead reviewed PR1-4's actual code and found 5 concrete gaps still open in
the testing infrastructure itself (not a code change under test). Full unit suite always runs.
`daily-sleep-skip` and `fsp-basic` needed a real live run - `coverage-index.json` had no entry for
either. `daily-basic` already had a real entry (Run 1, above) so it was left alone; re-running it
would just have burned a call proving nothing new. `fsp-basic` needed `coach-skanda-testing` reset
to a genuinely blank state first (see below) since it targets an athlete who hasn't done FSP.

### Unit suite

1019 passed, 0 failed, 6.17s. Raw: `test-results/raw/2026-09-14/unit/vitest-results-18-15-01.json`

### Simulation suite — paid, provider: openrouter, 11 calls, $0.128

| Scenario         | Result | Notes                                                                                                                                                                                                                                                                                                                            |
| ---------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| daily-sleep-skip | PASS   | 5/5 turns PASS vs coach-akash (`akash-suresh/coach-akash-suresh`), scratch branch `test/manual-2026-09-14T18-15-40-683Z`. First real run ever - `coverage-index.json` had no prior entry. Raw: `test-results/raw/2026-09-14/manual/manual-coach-chat-akash-suresh-coach-akash-suresh-log-18-17-00.json`                          |
| fsp-basic        | PASS   | 6/6 turns PASS vs `coach-skanda-testing`, run against a freshly-reset scratch branch (`test/1053-fsp-reset`, see below), not the default `--branch` the driver would otherwise auto-name. First real run ever. Raw: `test-results/raw/2026-09-14/manual/manual-coach-chat-skanda-testing-coach-skanda-testing-log-18-19-51.json` |

Real per-call usage reached both log entries for the first time (#1053 gap 2 - `askGemini()`'s
usage now threads through `RepliedTurn`/`TurnWrites`, and `run-manual-coach-chat-test.ts` reads it
back via `coachTurn.ts`'s new `getLastTurnUsage()`, module-level state read right after each
in-process `handle()` call). `daily-sleep-skip`: 5 calls, $0.06188325 (OpenRouter's own wire
`costUsd`, e.g. turn 0: `{promptTokens: 11544, completionTokens: 41, costUsd: 0.00881175}`).
`fsp-basic`: 6 calls (2 reprompts fired mid-run on turn 2's `unrecordedFacts`/`missedSeasonLanguage`
guard, summed correctly into one turn total), $0.06654525.

**`coach-skanda-testing` reset (fsp-basic prerequisite):** created `test/1053-fsp-reset` off
`origin/main` via local git (no permission gate hit, so the GitHub-API fallback wasn't needed),
overwrote the 5 FSP-owned files with `platform/scripts/carve-skeleton.mjs`'s exact blank templates
(`profile.json`, `memory.json`, `injuries.json`, `seasons.json`, `quests.json`), committed, pushed.
`origin/main` on that repo was never touched. Local checkout returned to `main` afterward.

**`test-results/coverage-index.json`** now has real, current entries for all 3 scenarios -
`manual:daily-basic` (from Run 1), `manual:daily-sleep-skip` and `manual:fsp-basic` (this run),
each with a real `last_pass_sha`, `watched_paths`, and (new) `last_cost_usd`.

**Selective re-run (#1053 gap 1) verified working:** re-ran `daily-sleep-skip` with no code change
since its pass above - `run-simulation-suite.ts` correctly skipped it (`git diff --quiet
c604a89 HEAD -- <watched paths>` empty) instead of spending another call:

```
[skip] daily-sleep-skip: no diff between c604a89 and HEAD across 8 watched path(s) (...); last passed 2026-09-14. Use --force to run anyway.
```

`--force --dry-run` on the same case correctly showed `[dry-run] ... would run` instead, confirming
`--force` bypasses the check as designed. No extra paid call was made proving this - both checks
used `--dry-run` or an already-skipped run.

**Scratch-branch cleanup (#1053 gap 4) revisited, not built.** A dedicated sweep script was built
and verified against real data (28 branches on akash, 130 on skanda-testing, `--older-than`
filtering confirmed correct) but then cut before this PR closed - nothing schedules vade-the-tester
to run it on any cadence, so the tool would still need a human to remember to invoke it, no less
manual than running the raw `gh api` commands directly (ADR 0023's principle: a signal only earns
automation when something other than a person keeps it current). `coach-chat-testing.md`'s Cleanup
section documents the manual `gh api` sweep instead.

**Gap 2 design decision, stated plainly:** usage does NOT reach `commitTurn()`'s athlete-facing
JSON response - that stays exactly as it was, no new field on real production API surface.
Instead, `coachTurn.ts` exports `getLastTurnUsage()`, module-level state set right after
`commitTurn`/`handleGreet` complete. `run-manual-coach-chat-test.ts` (single-threaded, one turn at
a time, calls `handle()` in-process) reads it back after each turn. This works because the manual
harness is an in-process caller, not an HTTP client - no side channel on the wire was needed at
all. Full account of what was/wasn't done, and why, is in the PR's own report.

**Run cost:** $0.128 (11 paid calls, all this run's simulation-suite section). **Day total so
far:** $0.128 (Runs 1-2 predate usage tracking and recorded $0; this is the first day total this
doc can actually sum from real numbers).
