# Test results - 2026-08-26

## Run 1 - manual, coach-skanda-2003, repeated retries under a Gemini outage

**Scope decided:** two scripted scratch-branch conversations against `skanda-2003/coach-skanda-2003` -
a short weight/hip-soreness/5k-run/wrap-up flow, and a longer profile-intake flow (age, height,
weight, timezone, goal, injuries, training frequency, wrap-up). No eval or unit runs this day.

### Manual live-chat - athlete: skanda-2003, repo: skanda-2003/coach-skanda-2003, model: not recorded (predates provider/model logging in these files)

14 manual log files this day, most of them re-attempts of the same two conversations. The day was
dominated by a sustained Gemini outage, not a product bug: almost every early turn came back
`Gemini request failed (504)` or `(503)` with no reply at all (`test-results/raw/2026-08-26/manual/manual-coach-chat-log-10-00-09.json`
through `manual-coach-chat-log-12-20-15.json`). Where a run's final "wrap this up" turn hit the
same outage, the harness recorded it as `FAIL` rather than `ERROR` with a named reason -
`expected session_closed=true, got false` (e.g.
`test-results/raw/2026-08-26/manual/manual-coach-chat-log-10-20-57.json` turn-4) - a direct,
verified consequence of the 504, not an independent bug.

No single file this day has every turn PASS - the closest is
`test-results/raw/2026-08-26/manual/manual-coach-chat-log-12-20-07.json`, where 4 of 6 turns
(profile intake through training frequency) landed cleanly before the final wrap-up turn hit
another outage-driven `FAIL`. A handful of individual turns elsewhere passed on retry (e.g. weight,
hip-soreness turns in `manual-coach-chat-log-11-49-02.json`, `manual-coach-chat-log-12-19-27.json`),
but no conversation closed end-to-end this day.

Root cause for every FAIL/ERROR this day: root cause not recorded beyond the Gemini
504/503 error text itself (infra-side outage/overload, not this repo's code) - no historical doc
covers this date.

No paid section has a usable cost figure: cost not recorded (predates usage tracking, #1045).
