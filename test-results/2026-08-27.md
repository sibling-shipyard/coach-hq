# Test results - 2026-08-27

## Run 1 - manual, coach-skanda-2003, same outage tailing off through the morning

**Scope decided:** the same two scripted conversations as the day before (weight/hip/5k/wrap-up,
and a name/DOB/session-query/wrap-up flow), plus one full unit-suite run. No eval this day.

### Unit suite
461 passed, 0 failed. Raw: `test-results/raw/2026-08-27/unit/vitest-results-17-46-43.json`

### Manual live-chat - athlete: skanda-2003, repo: skanda-2003/coach-skanda-2003, model: not recorded (predates provider/model logging)

10 manual log files. The Gemini 503/504 outage from 2026-08-26 continued into the morning but
cleared up as the day went on. One conversation completed cleanly end to end:
`test-results/raw/2026-08-27/manual/manual-coach-chat-log-10-43-45.json` - weight, hip soreness, and
the 5k run all landed, and the wrap-up turn committed `calisthenics_a.json`,
`chat_history.json`, `coach_log.json`, `memory.json`, and `profile.json`. Another,
`manual-coach-chat-log-11-24-49.json`, landed all four content turns (name, DOB uncertainty,
DOB confirmation) before its wrap-up turn hit the tail end of the outage.

Two "wrap this session" FAILs (`manual-coach-chat-log-11-07-02.json` turn-2,
`manual-coach-chat-log-10-44-00.json` turn-4) both carry the same named root cause as the day
before: a `Gemini request failed (503)` response, with `failures: ["expected session_closed=true,
got false"]` as the direct, verified consequence, not a separate bug. Every other FAIL/ERROR this
day traces to the same outage tail; root cause not recorded beyond that Gemini error text.

No paid section has a usable cost figure: cost not recorded (predates usage tracking, #1045).
