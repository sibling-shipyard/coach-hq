#!/usr/bin/env bash
# Optional local gate: run `ln -s ../../kdb/scripts/pre-commit .git/hooks/pre-commit` to enable.
# Blocks a commit if the knowledge base is malformed or the ADR index is stale.
exec python3 "$(git rev-parse --show-toplevel)/kdb/scripts/validate_kdb.py"
