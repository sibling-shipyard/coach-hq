#!/usr/bin/env node
/**
 * generate-wi-tokens.mjs — Thin wrapper so `npm run generate-tokens` and
 * build-data.mjs keep working. Emit logic lives in shared/warm-instrument/generate.mjs.
 */
import "../../shared/warm-instrument/generate.mjs";
